# 기여 안내

기억 원장은 대한민국 독립운동가·참전용사 등 인물 기록을 검증 가능하게 보존하는
공개 원장입니다. 기여는 두 경로로 나뉩니다 — **코드**와 **기록**은 다르게 다룹니다.

## 코드 기여 (GitHub PR)

1. 이슈로 먼저 논의하고, 브랜치를 만들어 PR을 보내주세요.
2. 테스트 필수: `cd contracts && npx hardhat test`, `cd server && npm test` 모두
   그린이어야 합니다. 새 동작에는 테스트를 함께 추가해주세요.
3. 원칙: 최소 의존성(서버는 stdlib 우선), 프론트 빌드 파이프라인 금지(브라우저
   JS 섬 4개 + vendored 라이브러리), EJS 출력은 이스케이프(`<%= %>`), SQL은
   바인딩만, 커밋 메시지에 Co-Authored-By 트레일러 금지.
4. 내부 구조는 `docs/architecture.md`를 먼저 읽어주세요.

## 기록 기여 (PR이 아닌 별도 경로)

역사적 사실의 추가·수정은 GitHub PR로 받지 않습니다. 웹의 **수정 요청** 경로를
사용해주세요:

1. 인물 페이지 → "수정 요청하기" — **실명, 연락처, 근거 출처 필수** (익명 불가
   원칙). 요청은 공개 목록에 노출됩니다.
2. 실질 변경은 심사(피어 리뷰)로 전환되어 검토자들의 공개 리뷰를 거칩니다 —
   정족수(승인 2, 미해결 반대 0) 충족 시에만 반영됩니다.
3. 반영된 기록은 변경 사유와 함께 새 버전으로 체인에 앵커되고, 이전 버전 이력은
   영구 공개됩니다.

거버넌스 규칙 전체는 `docs/architecture.md`의 "편집 거버넌스" 섹션 참고.

## 라이선스

기여하시면 AGPL-3.0-only 라이선스에 따라 배포되는 것에 동의하는 것으로 간주합니다.
